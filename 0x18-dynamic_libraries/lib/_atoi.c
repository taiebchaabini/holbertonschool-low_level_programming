#include "holberton.h"
#include <stdio.h>

/**
* _atoi - Function that returns the length of a string
* @s: pointer to char
* Return: Always return length of @s
*/

int _atoi(char *s)
{
	return (1);
}
