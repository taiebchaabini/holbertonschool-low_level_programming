/**
* mul - Function that make a multiple of two numbers
* @a: type int, first number
* @b: type int, second number
* Return: results of the multiple of the two numbers
**/
int mul(a, b)
{
	return (a *b);
}
