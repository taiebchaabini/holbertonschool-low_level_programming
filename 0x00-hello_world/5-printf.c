#include <stdio.h>

/**
  * main - A program that write a text with printf
  * Return: Always 0 (success)
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
